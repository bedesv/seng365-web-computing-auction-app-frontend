import {AppBar, Button, IconButton, makeStyles, Paper, Typography} from "@mui/material";
import CSS from 'csstype';
import Header from "./Header"


const card: CSS.Properties = {
    padding: "10px",
    margin: "20px",
}

const Home = () => {

    return (
        <div>
            <Header />
        </div>
    )
}
export default Home;