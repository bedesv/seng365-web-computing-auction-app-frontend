export type Auction = {
    auctionId: number,
    categoryId: number,
    endDate: Date,
    highestBid: number,
    numBids: number,
    reserve: number,
    sellerFirstName: string,
    sellerId: number,
    sellerLastName: string,
    title: string

}